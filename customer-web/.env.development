VITE_API_BASE_URL=http://localhost:5050/api/v1

# Public origin of the storefront, used for canonical / og:url / sitemap URLs.
VITE_SITE_URL=http://localhost:5173

# API base URL reachable from the BUILD MACHINE (Node), used by scripts/seo-*.mjs
# to generate sitemap.xml and the prerendered pages. Must be absolute.
VITE_SEO_API_BASE_URL=http://localhost:5050/api/v1
